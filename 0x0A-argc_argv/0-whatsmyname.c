#include<stdio.h>
/**
 * main - Entry point
 * This program prints its name
 * Return: 0 is returned
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s \n", argv[0]);
	return (0);
}
