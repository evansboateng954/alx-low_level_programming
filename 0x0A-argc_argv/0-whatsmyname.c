#include "main.h"
#include <stdio.h>
/**
 * main - entry point
 * @argc: the number of command line arguments
 * @argv: an array of command line arguments
 * Return: a new the name of the file
*/

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s \n", argv[0]);
	return (0);
}


