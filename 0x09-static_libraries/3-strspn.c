#include "main.h"
/**
 * _strspn - returns the length of substring that matches a given pattern
 * @s: the string to be scanned
 * @accept: the substring whose lenth is to be determined
 * Return: The length of a given substring
 */
unsigned int _strspn(char *s, char *accept)
{
}
